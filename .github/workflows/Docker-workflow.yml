name: CI/CD Docker Build

on:
  push:
    paths:
      - '**.java'
  workflow_dispatch:  # Allows manual workflow trigger

jobs:
  build_Docker_image:
    runs-on: ubuntu-latest  # Run the job on Ubuntu VM

    steps:
    - name: Checkout Code
      uses: actions/checkout@v2

    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'

    - name: Run Maven clean, compile, and test
      run: |
        mvn clean
        mvn compile
        mvn test

    - name: Build Docker Image
      run: |
        docker build -t github-calculator .

    - name: Push Docker Image to Docker Hub
      uses: docker/build-push-action@v2
      with:
        context: .
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/github-calculator:latest
